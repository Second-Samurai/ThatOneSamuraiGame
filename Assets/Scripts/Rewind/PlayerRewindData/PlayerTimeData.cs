using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerTimeData
{
    public bool lockedOn;

    public PlayerTimeData(bool _lockedOn) 
    {
        lockedOn = _lockedOn;
    }
}
